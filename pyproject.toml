[project]
name = "repl-nix-workspace"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.11"
dependencies = [
    "crewai>=1.7.2",
    "crewai-tools>=1.7.2",
    "flask>=3.1.2",
    "flask-cors>=6.0.2",
]
